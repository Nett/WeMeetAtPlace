resources:
  - ../../../base/user

namespace: production

patches:
  - target:
      kind: Deployment
      name: wemeetatplace-user
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 3
