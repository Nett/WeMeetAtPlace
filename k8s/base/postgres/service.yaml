apiVersion: v1
kind: Service
metadata:
  name: wemeetatplace-postgres
spec:
  selector:
    app: wemeetatplace-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP
